{
  "name": "horizongraph",
  "private": true,
  "license": "MIT",
  "scripts": {
    "gen": "pnpm -C tools gen",
    "validate": "pnpm -C tools validate",
    "build:data": "pnpm -C tools build",
    "build:web": "pnpm -C web build",
    "build": "pnpm gen && pnpm validate && pnpm build:data && pnpm build:web",
    "dev": "pnpm build:data && pnpm -C web dev"
  }
}